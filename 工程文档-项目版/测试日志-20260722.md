# CampusOS 实际测试原始日志摘录（2026-07-22）

## 网站构建

```text
> campus-web@1.0.0 build
> vue-tsc --noEmit && vite build
vite v5.4.21 building for production...
✓ 3856 modules transformed.
assets/index-PGPqYrIz.js  1,114.06 kB │ gzip: 369.68 kB
✓ built in 27.22s
(!) Some chunks are larger than 500 kB after minification.
```

## 小程序 Node 测试

```text
TAP version 13
ok 1 - tests\\format.test.mjs
ok 2 - tests\\request.test.mjs
# tests 2
# pass 2
# fail 0
# duration_ms 122.4088
```

## 后端领域规则

```text
openjdk version "17.0.19" 2026-04-21
OpenJDK Runtime Environment Temurin-17.0.19+10
DOMAIN_RULES_CHECK=PASS
```

执行命令：

```powershell
docker run --rm campusos-backend-test:local bash -lc "java -ea -cp campus-domain/target/test-classes:campus-domain/target/classes:campus-common/target/classes com.campus.domain.DomainRulesCheck"
```

## API 联调摘要

```text
TC-AUTH-01 正确登录          HTTP 200  code=0    token=True,user=admin
TC-AUTH-02 错误密码          HTTP 200  code=401
TC-AUTH-02 空字段            HTTP 200  code=400
TC-AUTH-03 未登录鉴权         HTTP 200  code=401
TC-NEWS-01 新闻列表          HTTP 200  code=0    count=10,total=10
TC-NEWS-02 新闻搜索          HTTP 200  code=0    keyword=我校,count=2
TC-NEWS-03 详情首次          HTTP 200  code=0    viewCount=363
TC-NEWS-03 详情刷新          HTTP 200  code=0    viewCount=364
TC-NEWS-04 收藏/列表/取消     HTTP 200  code=0    favorites=1
TC-NEWS-05 发布/下线/删除     HTTP 200  code=0    createdId=13
TC-AUTH-04 退出登录          HTTP 200  code=0
TC-AUTH-04 退出后旧Token访问  HTTP 200  code=0    FAIL（预期 code=401）
```

后端 SQL 日志确认搜索条件为 UTF-8 中文：

```text
Preparing: SELECT COUNT(*) FROM t_news WHERE deleted=0 AND (status = ? AND title LIKE ?)
Parameters: PUBLISHED(String), %我校%(String)
Row: 2
```

## 宿主机后端编译阻塞

```text
Java version: 11.0.23
Compiling 16 source files with javac [debug release 17]
BUILD FAILURE
错误: 不支持发行版本 17
```
